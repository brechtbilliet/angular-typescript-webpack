export class RegisterData {
    constructor(public emailAddress: string, public password: string, public confirmPassword: string,
                public firstName: string, public lastName: string) {
    }
}